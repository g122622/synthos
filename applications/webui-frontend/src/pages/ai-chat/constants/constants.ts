/**
 * AI Chat 页面常量配置
 */

/**
 * 默认标签页
 */
export const DEFAULT_ACTIVE_TAB = "ask";

/**
 * 默认检索数量
 */
export const DEFAULT_TOP_K = 100;

/**
 * 默认是否启用查询重写
 */
export const DEFAULT_ENABLE_QUERY_REWRITER = true;

/**
 * 默认语义搜索限制数量
 */
export const DEFAULT_SEARCH_LIMIT = 10;

/**
 * 默认侧边栏收起状态
 */
export const DEFAULT_SIDEBAR_COLLAPSED = false;
